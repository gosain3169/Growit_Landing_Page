const { HERO, NAV_SECTIONS, SCROLL_TOP_THRESHOLD, CAROUSEL, STATS, TESTIMONIALS } = GROWIT;

initProgressBar();
initScrollTop(SCROLL_TOP_THRESHOLD);
initHero(HERO);
initTheme();
initStats(STATS);
initReveal();
initNav(NAV_SECTIONS);
initCarousel(TESTIMONIALS, CAROUSEL);
initServices();
